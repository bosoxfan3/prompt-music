import styled from '@emotion/styled';

const LoadingSpinnerModal = styled('div')`
    text-align: center;

    & .loading-spinner-modal {
        &__text {
            margin: 1.5rem 2rem;
        }
    }
`;

export default LoadingSpinnerModal;
